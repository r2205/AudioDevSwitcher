<Window
    x:Class="AudioDevSwitcher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Audio Device Switcher"
    Width="480" Height="640"
    WindowStartupLocation="CenterScreen">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Output Devices -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8" LastChildFill="False">
            <TextBlock Text="Output Devices" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock x:Name="OutputHotkeyHint" DockPanel.Dock="Right"
                       Foreground="Gray" FontSize="11" VerticalAlignment="Bottom"/>
        </DockPanel>

        <ListView Grid.Row="1" x:Name="OutputList"
                  ItemsSource="{Binding OutputDevices}"
                  SelectedItem="{Binding SelectedOutputDevice, Mode=TwoWay}"
                  SelectionMode="Single"
                  Margin="0,0,0,16">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="&#x1F50A;" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" Text="&#x2713;" FontSize="16" FontWeight="Bold"
                                   Foreground="#0078D4" VerticalAlignment="Center"
                                   Visibility="{Binding IsDefault, Converter={StaticResource BoolToVis}}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Input Devices -->
        <DockPanel Grid.Row="2" Margin="0,0,0,8" LastChildFill="False">
            <TextBlock Text="Input Devices" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock x:Name="InputHotkeyHint" DockPanel.Dock="Right"
                       Foreground="Gray" FontSize="11" VerticalAlignment="Bottom"/>
        </DockPanel>

        <ListView Grid.Row="3" x:Name="InputList"
                  ItemsSource="{Binding InputDevices}"
                  SelectedItem="{Binding SelectedInputDevice, Mode=TwoWay}"
                  SelectionMode="Single"
                  Margin="0,0,0,16">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="&#x1F3A4;" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" Text="&#x2713;" FontSize="16" FontWeight="Bold"
                                   Foreground="#0078D4" VerticalAlignment="Center"
                                   Visibility="{Binding IsDefault, Converter={StaticResource BoolToVis}}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Settings -->
        <GroupBox Grid.Row="4" Header="Settings" Margin="0,0,0,12" Padding="8,4">
            <StackPanel>
                <CheckBox Content="Start with Windows"
                          IsChecked="{Binding StartWithWindows, Mode=TwoWay}"
                          Margin="0,4"/>
                <CheckBox Content="Start minimized to tray"
                          IsChecked="{Binding StartMinimized, Mode=TwoWay}"
                          Margin="0,4"/>
                <CheckBox Content="Play confirmation tone when switching"
                          IsChecked="{Binding PlayConfirmationTone, Mode=TwoWay}"
                          Margin="0,4"/>
            </StackPanel>
        </GroupBox>

        <!-- Bottom toolbar -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Refresh" Padding="16,6" Command="{Binding RefreshDevicesCommand}"/>
        </StackPanel>

        <!-- Version -->
        <TextBlock Grid.Row="6" x:Name="VersionLabel" HorizontalAlignment="Right"
                   Foreground="Gray" FontSize="11" Margin="0,8,0,0"/>
    </Grid>

</Window>
